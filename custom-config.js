module.exports = {
	/*
		if you want your build file to include a source-map (default: false)
	*/
	// buildSourceMap: 'inline-source-map',

	/*
		port to run dev server on (default: 4800)
	*/
	// port: 4800

	/*
		port to proxy api requests to
	*/
	// proxyPort: 1234
};